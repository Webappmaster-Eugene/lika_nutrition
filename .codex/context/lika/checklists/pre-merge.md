- `npm run smoke` зелёный целиком (lint, typecheck, build)
- Папка `out/` создана сборкой и содержит `index.html`, `robots.txt`, `sitemap.xml`
- Новая страница добавлена в `src/app/sitemap.ts` с трейлинг-слэшем и в навигацию
- Новый динамический маршрут имеет `generateStaticParams`
- Не появилось серверного кода: route handlers, `middleware.ts`, серверных
  экшенов, `revalidate` — статический экспорт их не поддерживает
- Новая переменная окружения добавлена в `.env.example`, `docker-compose.yml` и
  Dokploy UI; `NEXT_PUBLIC_*` прокинута как `ARG` в `Dockerfile`
- В `NEXT_PUBLIC_*` не попало ничего, что нельзя показать посетителю
- Новые изображения сжаты и лежат в `public/images/**`, пути проверены сборкой
- Секреты не попали ни в код, ни в логи, ни в отчёты; `.env` не в коммите
- Тексты для пользователя по-русски и без опечаток
- В коммитах нет чужой незакоммиченной работы
